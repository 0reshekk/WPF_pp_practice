<Page
    x:Class="WPF_pp_practice.Pages.ProductsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPF_pp_practice.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="ProductsPage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- Фильтры -->
        <StackPanel Orientation="Horizontal">
            <TextBox x:Name="SearchTextBox" Width="200" Margin="5"></TextBox>
            <ComboBox x:Name="SortComboBox" Width="100" Margin="5"></ComboBox>
            <ComboBox x:Name="TypeComboBox" Width="100" Margin="5"></ComboBox>
        </StackPanel>
        
        <!-- Список -->
        <ListView>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="80"></ColumnDefinition>
                        </Grid.ColumnDefinitions>

                        <Image Grid.Column="0" Source="{Binding PhotoPath}">
                        </Image>
                        
                        <StackPanel Orientation="Vertical" Grid.Column="1">
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} | {1}">
                                        <Binding Path="ProductTypes.Name"></Binding>
                                        <Binding Path="Name"></Binding>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                            <TextBlock Text="{Binding Article}"></TextBlock>
                            <TextBlock Text="{Binding StringMaterials}"></TextBlock>
                        </StackPanel>

                        <TextBlock Text="{Binding MinPrice}"></TextBlock>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
